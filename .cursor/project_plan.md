Create a CLI application that can read an image and extract the characters and print them into a combined output.

## Project Setup 

1. Create a venv environment called .venv
2. python -m venv .venv
3. activate the venv
4. 

## Work packages: 
1. Create an image loader
2. create a function to load and process a single image.
3. add options to process the output, either print it to the terminal, write it to a text file or a csv file. 
4. create a main script that can handle a single filepath or a directory containing multiple images. 
5. the output file should be used for all processed files from one folder.


## Test the function with the images in the test folder 
